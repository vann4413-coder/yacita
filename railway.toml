[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter './packages/**' build && pnpm --filter @yacita/api build"

[deploy]
startCommand = "node apps/api/dist/server.js"
restartPolicyType = "on_failure"
healthcheckPath = "/health"
healthcheckTimeout = 30
