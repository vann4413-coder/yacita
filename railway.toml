[build]
builder = "nixpacks"
buildCommand = "NODE_ENV=development pnpm install --frozen-lockfile && pnpm --filter './packages/**' build && cd apps/api && npx prisma generate && npx tsc -p tsconfig.json --skipLibCheck"

[deploy]
startCommand = "node apps/api/dist/server.js"
restartPolicyType = "on_failure"
healthcheckPath = "/health"
healthcheckTimeout = 30
